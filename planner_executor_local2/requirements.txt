# Core dependencies
openai>=1.0.0
python-dotenv>=1.0.0

# SDK (install from local path)
# pip install -e ../../sdk-python

# Optional: External CAPTCHA solvers
# 2captcha-python>=1.2.0
# capsolver>=1.0.0

# ============================================
# Local LLM Models (use with --local flag)
# ============================================

# For MLX models on Apple Silicon (recommended for M1/M2/M3/M4)
# pip install mlx-lm

# For HuggingFace models (use with --local --provider hf)
# pip install torch transformers
